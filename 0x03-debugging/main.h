#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
int largest_number(int a, intb, int c);
int covert_day(int month, int day);
void prin_remaining_days(int month, int day, int year);

#endif
